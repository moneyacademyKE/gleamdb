# v4.2 Candidate Verification — Immutable Source

**Date:** 2026-08-11
**Repository:** `moneyacademyKE/gleamdb`
**Candidate branch:** `stability/mcp-stdio-conformance`
**Candidate SHA:** `PENDING_FINAL_CANDIDATE_SHA`
**Release scope:** `embedded-core-v1` only
**Base release:** `v4.1.0` / `a61f1066b3c429eea0758adff94b2a9172d06797`

> This report is committed together with the candidate evidence. The final candidate SHA is filled in only after this report and the inclusion manifest are committed, then every gate below is re-run against that immutable commit.

## Candidate Contents

The source closure is enumerated in `docs/releases/v4.2-candidate-inclusion-manifest.md`.

- Core: explicit transaction-domain transformation, state-ownership projection, deterministic vector oracle/validation, and deadline-aware APIs.
- Reliability policy: pure continuation/checkpoint/retry/fallback/idempotency data; it does not itself perform effects or switch a provider.
- Experimental boundaries: sharding semantics and durability fault-injection coverage remain in the repository, but do not expand the certified closure.
- Claim control: broader-stability tooling is fail-closed and its required external profiles are not present.

## Reproducible Verification

Run from the repository root with the Homebrew BEAM toolchain on `PATH`:

1. `git diff --check`
2. `gleam format --check src test bench`
3. `gleam check`
4. `gleam docs build`
5. `gleam test`
6. `sh scripts/test_module_boundaries.sh`
7. `sh scripts/test_embedded_core_profile.sh`
8. `sh scripts/test_release_metadata.sh`
9. `GITHUB_REF=refs/tags/v4.2.0 sh scripts/verify_release_metadata.sh`
10. `sh scripts/verify_embedded_core_v1.sh`
11. `AARONDB_BROAD_PROMOTION_DIR=~/.opencrabs/projects/gleamdb/files/v4.2-broader-stability-final sh scripts/verify_broader_stability.sh` (expected **NO-GO**; it prevents an unsupported wider claim)
12. `gleam export hex-tarball`

## Results

| Gate | Result | Candidate evidence |
|---|---|---|
| Diff hygiene | Pending final SHA | Must exit 0 with no whitespace errors. |
| Format, type check, API docs | Pending final SHA | Must exit 0. |
| Full Erlang test suite | Pending final SHA | Must pass in the exact final checkout. |
| Boundary/profile fixture regressions | Pending final SHA | Must reject forbidden imports and malformed profile metadata. |
| Embedded-core verifier | Pending final SHA | Must emit `EMBEDDED_CORE_V1_OK`. |
| Metadata/tag parity | Pending final SHA | Must emit `RELEASE_METADATA_OK version=4.2.0 tag=v4.2.0`. |
| Broader-stability verifier | Expected NO-GO | Missing source-bound long-soak and upgrade/rollback evidence must keep those surfaces experimental. |
| Hex package build | Pending final SHA | Exported tarball must succeed; generated archive is never committed. |
| CI | Pending PR | GitHub Actions must pass on the reviewable candidate. |

## Deferred Experimental Surfaces

This release does **not** promote sharding, distributed queries, Raft, replication, HA, remote federation, storage/transport, MCP, long-soak, upgrade/rollback, destructive-storage, independent-host, or LAN/WAN fault tolerance. The precise missing evidence and next commands are recorded in `docs/reviews/v4.2-broader-stability-2026-08-11.md`.

## Release Handoff

No merge, tag, push, GitHub Release, or Hex publication has been performed by this report. Those are separate irreversible actions, requiring explicit user approval after the candidate branch is reviewed, CI is green, and this report has final-SHA evidence.
