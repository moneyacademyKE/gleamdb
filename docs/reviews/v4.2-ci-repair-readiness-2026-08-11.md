# v4.2 CI Repair — Verified PR Readiness

**Date:** 2026-08-11  
**PR:** [#18](https://github.com/moneyacademyKE/gleamdb/pull/18)  
**Branch:** `stability/mcp-stdio-conformance`  
**Published candidate SHA:** `1dd2736cb18f09e8d3391216d6a6ee2205db7532`

## Verified repair

The failed CI run checked the PR merge candidate and reported seven failures in `raft_durability_test`. Every failure was `Error(Io("enoent"))` while writing a durability store.

The cause was a hard-coded workstation path in the tests:

- `/Users/moe/Desktop/gleamdb/build/raft-durability-test.store`
- `/Users/moe/Desktop/gleamdb/build/raft-durability-test.backup`

GitHub runners do not have that directory. This was a test portability defect, not a Mnesia correctness failure.

The repair consists of three bounded changes:

| Path | Change |
|---|---|
| `test/aarondb/raft_durability_test.gleam` | Use repository-relative `build/raft-durability-test.{store,backup}` paths. |
| `scripts/with_ephemeral_mnesia.sh` | Add one small wrapper that allocates and removes a private Mnesia directory around a command. |
| `.github/workflows/ci.yml` | Invoke every BEAM-stateful test, certification, harness, benchmark, and soak command through that wrapper. |

## Local evidence

The exact consolidated gate was run after the repair:

`sh scripts/with_ephemeral_mnesia.sh sh scripts/verify_embedded_core_v1.sh`

| Gate | Result |
|---|---|
| Embedded-core boundary checker | Pass |
| Boundary regression fixture | Pass |
| Certification-profile regression | Pass |
| Gleam formatting | Pass |
| Gleam type check | Pass |
| API documentation build | Pass |
| Full test suite | **319 passed, 0 failures** |
| Diff hygiene | Pass |

## Current remote status

PR #18 remains **not mergeable** because GitHub only knows the prior published SHA. The existing CI run `31524487654` is failed, and its merge state is `UNSTABLE`.

The repair is intentionally still uncommitted and unpushed:

| Item | State |
|---|---|
| Published branch SHA | `1dd2736` |
| Local repair files | 2 modified, 1 new |
| New remote CI run for repair | Not started |
| Merge, tag, GitHub Release, Hex publish | Not performed |

## Required next action

Review the three-file diff, create one atomic repair commit, and push it to the existing PR branch. GitHub Actions must then pass on the resulting PR merge candidate before any merge, tag, or publish decision.

This report certifies only the repaired local evidence. It does **not** claim that the existing failed CI run is green, nor does it expand v4.2 beyond the bounded `embedded-core-v1` release scope.
