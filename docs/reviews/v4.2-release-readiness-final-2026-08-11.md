# v4.2 Release Readiness — Final Candidate Decision

**Date:** 2026-08-11
**Repository:** `moneyacademyKE/gleamdb`
**Candidate branch:** `stability/mcp-stdio-conformance`
**Candidate SHA:** `807019abcb868e1b727998d84683012970da51b3`
**Base release:** `v4.1.0` / `a61f1066b3c429eea0758adff94b2a9172d06797`
**Release scope:** `embedded-core-v1` only

## Decision

**NO-GO to publish v4.2.0 today.**

The source candidate is clean and locally verified. It is ready for an external review/CI step, but it is not yet eligible for merge, tagging, GitHub Release creation, or Hex publication because no remote draft PR or candidate-SHA GitHub Actions run exists. No irreversible release action was performed.

## Immutable Candidate

| Property | Value |
|---|---|
| Candidate SHA | `807019abcb868e1b727998d84683012970da51b3` |
| Worktree | Clean after final verification |
| Divergence from `origin/master` | 0 behind / 9 ahead |
| Version | `4.2.0` in `gleam.toml` |
| Release metadata | Tag parity passes for `v4.2.0` |
| Review traceability | Issue [#17](https://github.com/moneyacademyKE/gleamdb/issues/17) |
| Source allowlist | `docs/releases/v4.2-candidate-inclusion-manifest.md` |

The candidate comprises the earlier cluster-promotion boundary commits plus four disciplined v4.2 commits:

1. `7c3b5eb` — explicit core boundaries.
2. `f892f9c` — certified embedded-core release scope.
3. `fc976fd` — broader-stability promotion fails closed.
4. `cbb1519` and `807019a` — candidate evidence and its immutable-identity clarification.

## Candidate-SHA Evidence

All commands below ran against `807019a` with `PATH=/opt/homebrew/bin:$PATH`. Persistent logs are retained under:

`~/.opencrabs/projects/gleamdb/files/v4.2-final-candidate-807019abcb868e1b727998d84683012970da51b3/`

| Gate | Result |
|---|---|
| `git diff --check` | PASS |
| `gleam format --check src test bench` | PASS |
| `gleam check` | PASS |
| `gleam docs build` | PASS |
| `gleam test` | PASS — **319 passed, 0 failures** |
| `scripts/test_module_boundaries.sh` | PASS — forbidden import fixture rejected |
| `scripts/test_embedded_core_profile.sh` | PASS — malformed profile/artifact fixture rejected |
| `scripts/test_release_metadata.sh` | PASS — mismatched tag/changelog fixture rejected |
| Tag metadata verification | PASS — `RELEASE_METADATA_OK version=4.2.0 tag=v4.2.0` |
| `scripts/verify_embedded_core_v1.sh` | PASS — `EMBEDDED_CORE_V1_OK` |
| Distributed harness | PASS — `DISTRIBUTED_HARNESS_OK` |
| Benchmarks + bounded cluster soak | PASS — `CLUSTER_SOAK_OK` |
| Isolated Mnesia suite | PASS — **319 passed, 0 failures** |
| Workflow YAML parsing | PASS |
| Hex tarball export | PASS — `build/aarondb-4.2.0.tar` |
| Package artifact scan | PASS — no generated/runtime artifacts in the Hex archive |

The generated tarball and runtime/benchmark artifacts remain untracked and are excluded by the committed source allowlist.

## Certified Claim and Explicit Exclusions

v4.2 may claim **10/10 Rich Hickey structural certification for `embedded-core-v1` only**: data-oriented local Datalog models and deterministic algorithms, with a mechanical pure-to-adapter import boundary.

It must not claim broader stability for sharding, distributed queries, Raft, replication, HA, remote federation, storage/transport, MCP, long-soak, upgrade/rollback, destructive storage, independent hosts, or LAN/WAN faults.

The broader-stability verifier was intentionally exercised and returned the expected **NO-GO** because candidate-SHA `long-soak-v1` and `upgrade-rollback-v1` evidence is absent. Its witness is retained at:

`~/.opencrabs/projects/gleamdb/files/v4.2-broader-stability-final-807019abcb868e1b727998d84683012970da51b3/release-witness.json`

This is a correct constraint, not a test failure: it prevents a local core release from laundering itself into a distributed maturity claim.

## Remaining Release Gate

| Required before publication | Status |
|---|---|
| Push candidate branch and open draft PR | Not performed — requires explicit approval because it leaves the machine. |
| GitHub Actions on candidate SHA | Pending the pushed PR/branch. |
| Review/approval of release scope and changelog | Pending. |
| Merge to `master` | Not performed — requires explicit approval. |
| Signed or explicitly unsigned `v4.2.0` tag | Not performed — requires explicit approval. |
| Tag push, GitHub Release, Hex publication | Not performed — requires explicit approval. |

## Release Handoff

Once the user explicitly approves the external actions and candidate CI is green, the only remaining sequence is:

1. Push `stability/mcp-stdio-conformance` and open a draft PR against `master` with `fixes #17`.
2. Review the CI run on `807019a` (or the merge SHA if the branch changes).
3. Merge after explicit confirmation.
4. Create either a signed `v4.2.0` tag or explicitly accept an unsigned tag.
5. Push the tag; the release workflow verifies tag/version parity, creates the GitHub Release, and publishes to Hex only when the configured secret exists.

> **Rich Hickey certification:** release evidence now describes one clean immutable source and one bounded claim. The only missing ingredient is external review/CI—not another layer of bullshit abstraction.
