# v4.2 Broader-Stability Claim Review

**Date:** 2026-08-11
**Candidate source commit:** `59265a26c7b131e4c9f47d32be6f6aebbdcdb0dc`
**Worktree identity at verification:** `v4.1.0-4-g59265a2-dirty`
**Verdict:** **NO-GO for every broader-stability claim.**

## Executed Gate

`AARONDB_BROAD_PROMOTION_DIR=~/.opencrabs/projects/gleamdb/files/v4.2-broader-stability-2026-08-11 sh scripts/verify_broader_stability.sh`

The verifier exited `1` and recorded its machine-readable witness at:

`~/.opencrabs/projects/gleamdb/files/v4.2-broader-stability-2026-08-11/release-witness.json`

## Results

| Requirement | Status | Evidence |
|---|---|---|
| `long-soak-v1` profile definition | Present | SHA-256 `330003b3e13cc84921da2e0fb6101f5f2bd64ffb29265069138311cc4eec5f15` |
| `long-soak-v1` candidate evidence | **Missing** | Expected `artifacts/broader-stability/long-soak-v1/evidence.json` |
| `upgrade-rollback-v1` profile definition | Present | SHA-256 `949f15c049266d516acad168f8e5051c4a876eeb17569a5798af6180ae05c245` |
| `upgrade-rollback-v1` candidate evidence | **Missing** | Expected `artifacts/broader-stability/upgrade-rollback-v1/evidence.json` |
| Lifecycle drills | Pass | Local verifier reports backup/restore, rotation/revocation, rebuild, incident collection, and fail-closed checks |
| Diff hygiene | Pass | `git diff --check` |
| Source identity | **Fail** | Prepared candidate worktree is dirty, therefore not immutable evidence |

Additional destructive-storage evidence exists only as historical/generated local artifacts. Independent-host and LAN/WAN scripts and manuals exist, but there is no fresh candidate-SHA evidence package. None of those facts can be promoted into a v4.2 support claim.

## Allowed v4.2 Statement

v4.2.0 may claim the verified `embedded-core-v1` profile only. Sharding, Raft durability, distributed/cluster operation, HA, storage/transport, MCP, long-soak, upgrade/rollback, destructive-storage, independent-host, and LAN/WAN fault handling remain **experimental**.

CI enforces this split: normal v4.2 CI verifies release metadata and emits `RELEASE_SCOPE_OK scope=embedded-core-v1 broader-stability=experimental`. A branch that explicitly sets `AARONDB_RELEASE_SCOPE=broader-stability` runs `scripts/verify_broader_stability.sh`; without fresh valid evidence and a clean immutable worktree it fails.

## Reproducible Next Commands

1. Produce a named three-host `long-soak-v1` run that meets the versioned 24-hour / 1,000,000-operation SLO profile, then store its complete evidence manifest at `artifacts/broader-stability/long-soak-v1/evidence.json`.
2. Run the N/N-1 rolling upgrade, rollback, identity rotation, and restore procedure using a named topology, then store its evidence manifest at `artifacts/broader-stability/upgrade-rollback-v1/evidence.json`.
3. Commit the exact source, ensure a clean worktree, and run:

   `AARONDB_RELEASE_SCOPE=broader-stability sh scripts/verify_broader_stability.sh`

Only a resulting `BROAD_PROMOTION_GO` witness tied to that immutable candidate can change the experimental label.
