# Verification Witness — v4.2 Candidate

- **Timestamp (UTC):** 2026-08-11T18:11:43Z
- **Repository:** `moneyacademyKE/gleamdb` (`/Users/moe/Desktop/gleamdb`)
- **Candidate branch:** `stability/mcp-stdio-conformance`
- **Technical candidate SHA (pre-evidence record): `8077af5ea84dab6eda87a31da785a728213afd23`
- **Evidence-record commit:** `8077af5ea84dab6eda87a31da785a728213afd23`
- **Base release:** `v4.1.0` / `a61f1066b3c429eea0758adff94b2a9172d06797`
- **Linked issue:** [#17](https://github.com/moneyacademyKE/gleamdb/issues/17)
- **Verdict:** **PARTIAL** — source candidate is locally verified; external PR and technical-candidate GitHub Actions remain required before publication.

## Objective

Prepare a clean, reviewable v4.2.0 candidate with an explicit `embedded-core-v1` claim, mechanically fenced experimental surfaces, synchronized release metadata, and reproducible verification evidence. Do not merge, tag, push, create a GitHub Release, or publish to Hex.

## Files Changed

The candidate changes 144 paths relative to `origin/master`: 34 source files, 19 test files, 60 documentation files, 25 scripts, 2 CI workflow files, and 3 release-metadata files. The full file-by-file allowlist is committed at:

`docs/releases/v4.2-candidate-inclusion-manifest.md`

Key additions are:

- `docs/certification/embedded-core-v1.json` and `module-boundaries.edn`.
- `src/aarondb/transactor/domain.gleam`, `shared/ownership.gleam`, `continuation.gleam`, vector oracle/validation modules, and sharding semantics.
- Regression suites for continuation, ownership, transaction, vector, sharding, durability, and existing distributed boundaries.
- CI/release metadata verification plus fail-closed broader-stability runners.
- Release notes and review reports under `CHANGELOG.md`, `README.md`, and `docs/reviews/`.

No build output, local Mnesia state, `artifacts/`, test logs, or generated Hex tarball is included in the candidate source.

## Commands and Results

All commands ran with `PATH=/opt/homebrew/bin:$PATH`; persistent logs are at:

`~/.opencrabs/projects/gleamdb/files/v4.2-final-candidate-8077af5ea84dab6eda87a31da785a728213afd23/`

| Command | Exit/result |
|---|---|
| `git diff --check` | PASS |
| `gleam format --check src test bench` | PASS |
| `gleam check` | PASS |
| `gleam docs build` | PASS |
| `gleam test` | PASS — **319 passed, 0 failures** |
| `sh scripts/test_module_boundaries.sh` | PASS — forbidden import fixture rejected |
| `sh scripts/test_embedded_core_profile.sh` | PASS — malformed profile/artifact fixture rejected |
| `sh scripts/test_release_metadata.sh` | PASS — mismatched tag/changelog fixture rejected |
| `GITHUB_REF=refs/tags/v4.2.0 sh scripts/verify_release_metadata.sh` | PASS — `RELEASE_METADATA_OK` |
| `sh scripts/verify_embedded_core_v1.sh` | PASS — `EMBEDDED_CORE_V1_OK` |
| `sh scripts/verify_distributed_harness.sh` | PASS — `DISTRIBUTED_HARNESS_OK` |
| CI-equivalent benchmarks, bounded cluster soak, isolated Mnesia suite | PASS — `CLUSTER_SOAK_OK`; isolated suite **319 passed** |
| `gleam export hex-tarball` and artifact scan | PASS — no generated/runtime artifacts in the package |
| Broader-stability verifier | Expected **NO-GO** — missing candidate-SHA `long-soak-v1` and `upgrade-rollback-v1` evidence retains exclusions |

## Scope and Residual Risk

The candidate may claim 10/10 Rich Hickey structural certification only for `embedded-core-v1`: explicit local data models, deterministic algorithms, pure-to-adapter boundary enforcement, and fenced compatibility APIs.

It cannot claim sharding, distributed queries, Raft, replication, HA, remote federation, storage/transport, MCP, destructive storage, independent-host, LAN/WAN, long-soak, or upgrade/rollback readiness. Those claims require their own fresh candidate-SHA evidence.

The local `gh` CLI invocation is blocked by the host `rtk` wrapper, so no remote PR inspection or creation was performed in this session. GitHub Actions has not yet run against the candidate because the branch has not been pushed. These are publication blockers, not source-code failures.

## Required Handoff

1. Explicit approval to push the review branch and create a draft PR with `fixes #17`.
2. Green GitHub Actions on the candidate SHA or resulting merge SHA.
3. Explicit approval for merge, signed-or-explicitly-unsigned `v4.2.0` tag, tag push, GitHub Release, and optional Hex publication.

> **Rich Hickey certification:** the candidate’s claim is now bounded by executable structure and evidence. The remaining work is review and release authority, not pretending local proof is a distributed guarantee.
